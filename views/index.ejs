<%- include("templates/header") %>

    <% if (!user) { %>
        <div class="text-center">
            <a href="/signup" class="btn btn-primary m-2">Sign Up</a>
            <a href="/login" class="btn btn-secondary m-2">Login</a>
        </div>
        <% } else { %>
            <h2>Hello, <%= user.name %>
            </h2>
            <a href="/members" class="btn btn-primary m-2">Members</a>
            <a href="/logout" class="btn btn-danger m-2">Logout</a>
            <% } %>

                <%- include("templates/footer") %>